# 온리 프로젝트 대화 기록

마지막 업데이트: 2026-08-03

이 문서는 프로젝트를 만들며 나눈 사용자와 Codex의 대화를 요구사항과 결정 중심으로 기록한 것입니다. 시스템 메시지나 내부 작업 로그는 포함하지 않습니다.

## 1. 최초 아이디어

**사용자**

> 아이폰 용 자산 대시보드 : 주식 / 가계부 / 암호화폐 / 부동산 탭

사용자는 다크 테마의 참고 화면 네 장을 제공했다. 참고 화면에는 전체 자산 요약, 가족별 자산 비중, 카드사 엑셀 업로드 기반 가계부, 부부 공동 일정 기능이 포함되어 있었다.

**Codex**

- 참고 화면을 바탕으로 아이폰 화면에 맞는 자산 대시보드를 구현하기로 했다.
- 워크스페이스가 비어 있어 먼저 모바일 웹앱 프로젝트를 구성했다.

## 2. 앱스토어 없이 부부만 사용하는 방법

**사용자**

> 근데 아이폰용 어플리케이션 배포 없이 그냥 로컬에서 만들어서 우리 부부만을 위한 어플 개발이 가능할까?

**Codex**

- 앱스토어 공개 없이 사용할 수 있다고 답했다.
- 무료 Apple 계정으로 네이티브 앱을 직접 설치하면 주기적인 재설치가 필요하므로, Safari 홈 화면에 설치하는 비공개 웹앱(PWA)이 더 현실적이라고 제안했다.
- 두 아이폰이 같은 데이터를 보려면 동기화용 저장소가 필요하다고 설명했다.

## 3. Google Calendar와 서버 구상

**사용자**

> 그건 Google 캘린더 하면 되는 거 아닌가? 비공개 웹앱으로 해주고, 인터넷 연결된 서버는 있어. 윈도우 깔려있는.

**Codex**

- 일정은 새로 구현하기보다 부부 공유 Google Calendar와 연결하는 방향에 동의했다.
- 자산과 가계부는 비공개 저장소, 일정은 Google Calendar가 담당하는 구조로 개발을 시작했다.
- 카드 내역 엑셀 자동 분류, 금액 숨김, 가족별·투자·부동산 화면, 일정 추가, 접근 키 보호를 구현했다.

## 4. 프로젝트 위치 변경과 웹앱 결정

**사용자**

> /sungkunjeong/coding 밑에 프로젝트 생성해줘.

> 윈도우 서버는 안 하고 그냥 웹앱으로 해도 되지 않아?

**Codex**

- macOS 홈 경로에 맞춰 프로젝트를 `/Users/sungkunjeong/coding/only-family-assets`에 생성했다.
- 별도 윈도우 서버 없이 관리형 웹 호스팅과 PostgreSQL을 사용하는 비공개 웹앱 구조로 변경했다.
- 앱은 HTTPS 주소로 배포한 뒤 두 아이폰 Safari에서 홈 화면에 추가하는 방식으로 사용하기로 했다.

## 5. 확정된 기술 스택

**사용자**

> 기술 스택은 어떻게 되는 거야? 그리고 주식도 업로드할 수 있도록 해줘.

**Codex**

- Next.js 16, React 19, TypeScript 기반이라고 설명했다.
- 반응형 CSS와 PWA 메타데이터를 사용했다.
- 공유 데이터는 PostgreSQL, 엑셀 처리는 SheetJS `xlsx`, 일정은 Google Calendar API로 구성했다.
- 접근 키, HTTPS, 검색엔진 차단을 보안 기본값으로 설정했다.
- 주식/ETF 탭에 증권사 `.xlsx`, `.xls`, `.csv` 업로드 기능을 추가했다.
- 종목명, 티커, 수량, 평균단가, 현재가, 평가금액, 통화, 명의를 자동 인식하도록 구현했다.
- 업로드한 종목을 기준으로 총 원금, 평가금액, 수익률을 다시 계산하도록 구현했다.
- 다운로드 가능한 샘플 파일 `public/templates/stock-holdings.csv`를 추가했다.

## 6. 현재 요청

**사용자**

> 1. 이렇게 우리가 대화한 내역들을 chat.md에 올려주고,
> 2. 어떤 양식으로 올려야 업로드되는지 확인 좀.
> 3. 어떻게 모바일에서 여는지 알려주고
> 4. 마이데이터로 주식 종목이랑 비중을 쉽게 CSV로 다운로드하는 방법 없는지 검토해줘.

**Codex 작업 내용**

- 이 `chat.md`를 생성했다.
- 주식 샘플 CSV를 실제 SheetJS 파서로 검사했다.
- 최초 검사에서 한글 CSV 헤더의 인코딩 문제를 발견해 UTF-8 BOM 형식으로 수정했다.
- 수정 후 종목 3개와 7개 열이 모두 정상 인식되는 것을 확인했다.
- 현재 로컬 개발 서버가 Mac의 모든 네트워크 인터페이스에서 3000번 포트로 실행 중임을 확인했다.
- 금융 마이데이터 직접 CSV 취득 가능 여부와 증권사 잔고 다운로드 대안을 공식 자료 중심으로 검토했다.

## 현재 합의된 제품 범위

- 요약 및 가족별 자산 비중
- 주식/ETF 업로드와 손익 계산
- 카드사 내역 업로드 기반 가계부
- 암호화폐와 부동산 자산 관리
- Google Calendar 공동 일정
- 부부 두 사람만 접근하는 비공개 웹앱
- FastAPI API와 SQLite 누적 저장소
- 아이폰 홈 화면 웹앱 설치

## 아직 필요한 외부 설정

- 공개되지 않은 HTTPS 웹 주소에 배포
- 충분히 긴 `APP_ACCESS_KEY` 설정
- 공개 접속 시 `APP_ACCESS_KEY`, HTTPS 또는 비공개 터널 설정
- Google Calendar 서비스 계정과 공유 캘린더 설정

## 7. 뱅크샐러드 통합 데이터 반영

**사용자**

> 뱅크샐러드의 가계부 및 자산 현황이야. 이걸로 업로드하면 가계부 및 자산 포트폴리오 업데이트해줘.

**Codex 작업 내용**

- 제공된 `2025-08-03~2026-08-03.xlsx`의 `뱅샐현황`, `가계부 내역` 시트를 검사했다.
- 이체는 계좌 사이의 이동이라 지출에서 제외하고, 지출·수입만 가계부에 반영하도록 했다.
- 현금, 투자, 부동산, 동산, 부채를 합산해 요약·가족별·부동산 화면의 샘플 숫자를 실제 값으로 교체했다.
- 투자현황의 상품 37개를 주식/ETF 탭에 평가금액과 투자원금 기준으로 반영했다.
- 뱅크샐러드 통합 파일을 다시 업로드하면 기존 스냅샷을 교체하도록 구현했다.
- 로컬 실행 시 데이터는 Git에서 제외된 `data/private-state.json`에 저장하도록 변경했다.

## 8. 기존 포트폴리오 기능 이식

**사용자**

> `/coding/portfolio`에서 구현한 AI 분석, 환율, 현재가 갱신, 외부 연동 상태 확인, 목표가 Telegram 연동을 이 프로젝트로 옮겨 달라. 백엔드는 FastAPI로 구성하고 누적으로 저장하기 위해 SQLite DB로 만들어 달라.

> 아니 내 말은 그 프로젝트의 기능을 이 프로젝트로 옮겨달라는 이야기였어.

**Codex 작업 내용**

- 실제 실행 프로젝트가 `/Users/sungkunjeong/coding/only-family-assets`임을 확인하고 기존 화면과 사용자 변경사항을 보존한 채 기능을 병합했다.
- FastAPI 백엔드와 SQLite 누적 저장소를 추가했다.
- 뱅크샐러드 거래 중복 방지, 자산·종목·부채 최신 상태, 포트폴리오 스냅샷 이력을 구현했다.
- USD/KRW 환율과 국내외 종목 현재가 갱신, AI/로컬 포트폴리오 분석, 외부 연동 상태, Telegram 목표가 알림을 주식·설정 화면에 연결했다.
- 제공된 뱅크샐러드 파일로 가계부 1,782건과 투자상품 37개를 저장하고, 같은 파일 재업로드 시 신규 거래가 0건임을 확인했다.

## 9. Windows PC와 Tailscale 무료 운영

**사용자**

> Windows 서버가 아니라 Windows PC인데 무료 웹 서비스나 호스팅 업체는 없을까?

> 그렇게 해줘. PC는 계속 켤 수 있어.

**Codex 작업 내용**

- SQLite를 무료 호스팅의 임시 파일시스템에 두지 않고 Windows PC에 안전하게 유지하는 방식을 선택했다.
- Next.js와 FastAPI를 운영 모드로 함께 실행하는 `start:full` 스크립트를 추가했다.
- Windows 최초 설치, Tailscale Serve 비공개 HTTPS 연결, 로그인 시 자동 실행, 수동 중지 스크립트를 추가했다.
- 공유기 포트를 열지 않고 가족의 Tailscale 기기에서만 접속하도록 운영 지침을 문서화했다.

## 10. 가족 구성 정정

**사용자**

> 성근, 지우, 윤재 이렇게 가족은 있어.

**Codex 작업 내용**

- 앱의 가족 구성을 성근·지우·윤재로 변경하고 기존 지은 표기를 제거했다.
- 자산 업로드 소유자와 일정 공유 대상에 성근·지우·윤재·공통을 적용했다.
- 가족별 자산 화면이 세 가족 구성원의 자산 비중을 SQLite 소유자별로 계산하도록 변경했다.
- 앱 이름과 잠금·설정·PWA 설명을 `우리 가족의 자산` 기준으로 수정했다.
